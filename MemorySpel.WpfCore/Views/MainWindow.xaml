<Window x:Class="MemorySpel.WpfCore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:MemorySpel.WpfCore.Views"
        mc:Ignorable="d"
        Title="Memoryspel - WPF Core" ResizeMode="NoResize" WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <StackPanel>
                <TextBlock Text="{Binding Path=ElapsedTime, StringFormat=Elapsed time: {0}s}" />
            </StackPanel>
        </Grid>
        <views:MemoryCards x:Name="MemoryCardsControl" Grid.Column="1"/>
    </Grid>
</Window>
